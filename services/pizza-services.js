import config from './dbconfig.js';
import sql from 'mssql';

export class pizzaSerice {
    static  getAll = async(id) => {

    }

    static  getById = async(id) => {
        
    }

    static  insert = async(pizza) => {
        
    }

    static  update = async(pizza) => {
        
    }

    static  deleteBy = async(pizza) => {
        
    }

}